{
  "name": "moscow-time",
  "version": "0.1.0",
  "description": "Moscow Time",
  "main": "index.js",
  "repository": "https://github.com/siim-m/moscow-time.git",
  "author": "Siim",
  "license": "MIT",
  "scripts": {
    "copy": "mkdir -p public && cp *.* ./public/",
    "minify": "minify index.html > ./public/index.html && minify widget-no-frame.js > ./public/widget-no-frame.js && minify widget-no-frame.css > ./public/widget-no-frame.css && minify widget-with-frame.js > ./public/widget-with-frame.js && minify widget-with-frame.css > ./public/widget-with-frame.css",
    "build": "yarn copy && yarn minify"
  },
  "devDependencies": {
    "minify": "^7.0.2"
  }
}
